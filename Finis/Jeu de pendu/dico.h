#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

// Pioche un mot au hasard dans le dictionnaire.
int piocherMot(char *motChoisi);

// Fournit un nombre aléatoire pour choisir un mot.
int nombreAleatoire(int nombreMax);
